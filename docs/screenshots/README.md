# Screenshots

Add current portfolio/demo screenshots here after the real hosted deployment is live.

Recommended filenames:
- `openapi-docs.png`
- `streamlit-ui.png`
- `approval-flow.png`

Guidelines:
- capture the current UI/docs only after the hosted path is verified
- avoid showing secrets or the shared demo password
- refresh screenshots when the hosted review flow materially changes
